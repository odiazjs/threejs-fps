services:
  - type: web
    name: threejs-fps-server
    runtime: node
    rootDir: server
    # Root install first — shared/ resolves @dimforge/rapier3d-compat from repo root (single WASM copy).
    buildCommand: npm install --prefix .. && npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: MIGRATE_ON_START
        value: "true"
      - key: CORS_ORIGIN
        fromService:
          type: web
          name: threejs-fps-client
          envVarKey: RENDER_EXTERNAL_URL

  - type: web
    name: threejs-fps-client
    runtime: static
    buildCommand: npm install && npm run build:client
    staticPublishPath: dist
    envVars:
      # Baked into the JS bundle at build time. If empty, the client falls back to
      # deriving https://<name>-server.onrender.com from the static site hostname.
      - key: VITE_COLYSEUS_URL
        fromService:
          type: web
          name: threejs-fps-server
          envVarKey: RENDER_EXTERNAL_URL
