/** Shield recharges a player can carry in inventory. */
export const MAX_SHIELD_CHARGES = 6;

/** Shield recharges each player starts with. */
export const DEFAULT_SHIELD_CHARGES = 2;

/** Throwable grenades a player can carry. */
export const MAX_GRENADES = 6;

/** Grenades each player starts with. */
export const DEFAULT_GRENADES = 2;
