/** Shield recharge pickup world mesh (lod_shield_recharge.fbx in `3d/`). */
export const SHIELD_CHARGE_VISUAL_MODEL = 'lod_shield_recharge.fbx';

/** Preferred LOD when the FBX embeds multiple LOD meshes. */
export const SHIELD_CHARGE_VISUAL_LOD = 0;

/** Target world height after auto-scaling (meters). */
export const SHIELD_CHARGE_TARGET_HEIGHT = 0.55;
