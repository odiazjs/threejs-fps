/** Max distance (m) to show another player's username. */
export const REMOTE_USERNAME_MAX_DISTANCE = 5;

/** Half-angle cone (degrees) in front of the camera for username visibility. */
export const REMOTE_USERNAME_LOOK_ANGLE_DEG = 38;

/** Hide health bar after this many seconds without combat with that player. */
export const REMOTE_HEALTHBAR_COMBAT_TIMEOUT_SEC = 5;
