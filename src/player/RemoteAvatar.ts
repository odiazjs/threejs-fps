import { EYE_HEIGHT } from '../../shared/level/levelData';

/** Vertical pivot for remote UI (health bar, damage numbers). */
export const REMOTE_AIM_HEIGHT = EYE_HEIGHT - 0.06;

/** Distance from lookRig origin to the top of the character mesh. */
export const REMOTE_HEAD_TOP_OFFSET = 0.18;