# Colyseus server URL (read at Vite build time — set this on Render for the static site)
VITE_COLYSEUS_URL=http://localhost:4001

# Allowed browser origin for auth API CORS (Vite dev server by default)
# CORS_ORIGIN=http://localhost:5173
